handle duplicate users (hell)
    no same socket id in the room
    no same username in one room
    no same socket id in the all rooms

handle host switching (DONE): (add a bit of delay before switching the host)
    add context menu for managing players:
        kick, mute, make host